# Monitoring

`check_runtime.py` verifies that the final acceptance report, live target file and signal diagnostics exist and are mutually usable. It is intentionally read-only and can be scheduled by Windows Task Scheduler after target generation/execution.
